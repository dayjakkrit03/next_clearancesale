generator client {
  provider        = "prisma-client-js"
  output          = "../prisma/generated/interlink"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_INTERLINK")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model category {
  category_id          Int      @id @default(autoincrement())
  category_name        String   @db.VarChar(200)
  category_number      Int
  category_keyword     String?  @db.Text
  category_title       String?  @db.Text
  category_description String?  @db.Text
  category_color       String?  @db.VarChar(7)
  category_picture     String?  @db.VarChar(200)
  category_status      Int      @default(1)
  users_action         Int
  created_at           DateTime @default(now()) @db.Timestamp(0)
  updated_at           DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model more_pictures {
  mp_id           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id      Int      @default(0)
  product_picture String   @db.Text
  create_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name     String   @default("") @db.VarChar(250)
  update_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name     String   @default("") @db.VarChar(250)
}

model part {
  part_id      Int      @id @default(autoincrement())
  category_id  Int
  sub_id       Int
  part_name    String   @db.VarChar(200)
  part_picture String?  @db.VarChar(200)
  part_color   String?  @db.VarChar(7)
  part_status  Int      @default(1)
  users_action Int
  created_at   DateTime @default(now()) @db.Timestamp(0)
  updated_at   DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product {
  product_id          Int      @id @default(autoincrement())
  category_id         Int?     @default(0)
  sub_id              Int?     @default(0)
  part_id             Int?     @default(0)
  product_name        String?  @db.VarChar(255)
  product_brand       String?  @db.VarChar(255)
  product_description String?  @db.Text
  product_picture     String?  @db.Text
  product_sku         String?  @db.VarChar(255)
  product_file        String?  @db.Text
  product_filename    String?  @db.VarChar(255)
  product_price       Decimal? @db.Decimal(10, 2)
  product_new         Int?     @default(0)
  product_best        Int?     @default(0)
  product_status      Int?     @default(1)
  users_action        Int?
  created_at          DateTime @default(now()) @db.Timestamp(0)
  updated_at          DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  product_uom         String?  @db.VarChar(10)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 09 Boolean @default(true) @map("09")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  clearanceSales      Boolean? @default(false)
  clearanceQuantity   Int?     @default(0)
  clearancePrice      Decimal? @db.Decimal(10, 2)
  expo_status         Int?     @default(0) @db.TinyInt
  expo_price          Decimal? @default(0.00) @db.Decimal(10, 2)
  cat5e               Int?     @default(0) @db.TinyInt
  cat6                Int?     @default(0) @db.TinyInt
  tool_tester         Int?     @default(0) @db.TinyInt
}

model sub {
  sub_id          Int      @id @default(autoincrement())
  category_id     Int
  sub_name        String   @db.VarChar(255)
  sub_keyword     String?  @db.Text
  sub_title       String?  @db.Text
  sub_description String?  @db.Text
  sub_picture     String?  @db.VarChar(200)
  sub_color       String?  @db.VarChar(7)
  sub_status      Int      @default(1)
  users_action    Int
  created_at      DateTime @default(now()) @db.Timestamp(0)
  updated_at      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model category_clearance {
  category_id          Int      @id @default(autoincrement())
  category_name        String   @db.VarChar(200)
  category_number      Int
  category_keyword     String?  @db.Text
  category_title       String?  @db.Text
  category_description String?  @db.Text
  category_color       String?  @db.VarChar(7)
  category_picture     String?  @db.VarChar(200)
  category_status      Int      @default(1)
  users_action         Int
  created_at           DateTime @default(now()) @db.Timestamp(0)
  updated_at           DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model discountpercentage_clearance_tb {
  dcp_id           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id       Int      @default(0)
  product_discount String   @db.Text
  create_date      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name      String   @default("admin") @db.VarChar(250)
  update_date      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name      String   @default("admin") @db.VarChar(250)
}

model discountpercentage_tb {
  dcp_id           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id       Int      @default(0)
  product_discount String   @db.Text
  create_date      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name      String   @default("admin") @db.VarChar(250)
  update_date      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name      String   @default("admin") @db.VarChar(250)
}

model more_pictures_clearance {
  mpc_id          BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id      Int      @default(0)
  product_picture String   @db.Text
  create_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name     String   @default("") @db.VarChar(250)
  update_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name     String   @default("") @db.VarChar(250)
}

model more_pictures_test {
  mpt_id          BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id      Int      @default(0)
  product_picture String   @db.Text
  create_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name     String   @default("") @db.VarChar(250)
  update_date     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name     String   @default("") @db.VarChar(250)
}

model part_clearance {
  part_id      Int      @id @default(autoincrement())
  category_id  Int
  sub_id       Int
  part_name    String   @db.VarChar(200)
  part_picture String?  @db.VarChar(200)
  part_color   String?  @db.VarChar(7)
  part_status  Int      @default(1)
  users_action Int
  created_at   DateTime @default(now()) @db.Timestamp(0)
  updated_at   DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model producoptions_clearance_tb {
  pot_id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id     Int      @default(0)
  product_option String   @db.Text
  create_date    DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name    String   @default("admin") @db.VarChar(250)
  update_date    DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name    String   @default("admin") @db.VarChar(250)
}

model producoptions_tb {
  pot_id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  product_id     Int      @default(0)
  product_option String   @db.Text
  create_date    DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  create_name    String   @default("admin") @db.VarChar(250)
  update_date    DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  update_name    String   @default("admin") @db.VarChar(250)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product_clearance {
  product_id          Int      @id @default(autoincrement())
  category_id         Int?     @default(0)
  sub_id              Int?     @default(0)
  part_id             Int?     @default(0)
  product_name        String?  @db.VarChar(255)
  product_brand       String?  @db.VarChar(255)
  product_description String?  @db.Text
  product_picture     String?  @db.Text
  product_sku         String?  @db.VarChar(255)
  product_file        String?  @db.Text
  product_filename    String?  @db.VarChar(255)
  product_price       Decimal? @db.Decimal(10, 2)
  product_new         Int?     @default(0)
  product_best        Int?     @default(0)
  product_status      Int?     @default(1)
  users_action        Int?
  created_at          DateTime @default(now()) @db.Timestamp(0)
  updated_at          DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  product_uom         String?  @db.VarChar(10)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 09 Boolean @default(true) @map("09")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  clearanceSales      Boolean? @default(false)
  clearanceQuantity   Int?     @default(0)
  clearancePrice      Decimal? @db.Decimal(10, 2)
  expo_status         Int?     @default(0) @db.TinyInt
  expo_price          Decimal? @default(0.00) @db.Decimal(10, 2)
  cat5e               Int?     @default(0) @db.TinyInt
  cat6                Int?     @default(0) @db.TinyInt
  tool_tester         Int?     @default(0) @db.TinyInt
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 13 Boolean @default(true) @map("13")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product_test_upload {
  product_id          Int      @id @default(autoincrement())
  category_id         Int?     @default(0)
  sub_id              Int?     @default(0)
  part_id             Int?     @default(0)
  product_name        String?  @db.VarChar(255)
  product_brand       String?  @db.VarChar(255)
  product_description String?  @db.Text
  product_picture     String?  @db.Text
  product_sku         String?  @db.VarChar(255)
  product_file        String?  @db.Text
  product_filename    String?  @db.VarChar(255)
  product_price       Decimal? @db.Decimal(10, 2)
  product_new         Int?     @default(0)
  product_best        Int?     @default(0)
  product_status      Int?     @default(1)
  users_action        Int?
  created_at          DateTime @default(now()) @db.Timestamp(0)
  updated_at          DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  product_uom         String?  @db.VarChar(10)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 09 Boolean @default(true) @map("09")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  clearanceSales      Boolean? @default(false)
  clearanceQuantity   Int?     @default(0)
  clearancePrice      Decimal? @db.Decimal(10, 2)
  expo_status         Int?     @default(0) @db.TinyInt
  expo_price          Decimal? @default(0.00) @db.Decimal(10, 2)
  cat5e               Int?     @default(0) @db.TinyInt
  cat6                Int?     @default(0) @db.TinyInt
  tool_tester         Int?     @default(0) @db.TinyInt
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 13 Boolean @default(true) @map("13")
}

model sub_clearance {
  sub_id          Int      @id @default(autoincrement())
  category_id     Int
  sub_name        String   @db.VarChar(255)
  sub_keyword     String?  @db.Text
  sub_title       String?  @db.Text
  sub_description String?  @db.Text
  sub_picture     String?  @db.VarChar(200)
  sub_color       String?  @db.VarChar(7)
  sub_status      Int      @default(1)
  users_action    Int
  created_at      DateTime @default(now()) @db.Timestamp(0)
  updated_at      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model discount_rules {
  id               Int                             @id @default(autoincrement()) @db.UnsignedInt
  min_percent      Int                             @db.UnsignedTinyInt
  max_percent      Int?                            @db.UnsignedTinyInt
  border_width     Int                             @default(2) @db.UnsignedTinyInt
  border_color_hex String                          @db.Char(7)
  badge_bg_hex     String?                         @db.Char(7)
  badge_text_hex   String?                         @db.Char(7)
  display_order    Int                             @default(0)
  enabled          Boolean                         @default(true)
  frame_mode       discount_rules_frame_mode       @default(draw)
  frame_image_url  String?                         @db.VarChar(255)
  frame_inset_px   Int                             @default(0)
  frame_opacity    Decimal                         @default(1.00) @db.Decimal(3, 2)
  frame_object_fit discount_rules_frame_object_fit @default(contain)
  created_at       DateTime                        @default(now()) @db.Timestamp(0)
  updated_at       DateTime?                       @default(now()) @db.Timestamp(0)

  @@index([enabled], map: "idx_discount_rules_enabled")
  @@index([display_order], map: "idx_discount_rules_order")
}

model discount_rules_meta {
  id         Int      @id @default(1) @db.UnsignedTinyInt
  title      String   @db.VarChar(255)
  subtitle   String   @db.VarChar(255)
  updated_at DateTime @default(now()) @db.Timestamp(0)
}

model featured_list_items {
  id                 Int                @id @default(autoincrement()) @db.UnsignedInt
  list_id            Int                @db.UnsignedInt
  product_id         Int                @db.UnsignedInt
  display_order      Int                @db.UnsignedInt
  featured_lists     featured_lists     @relation(fields: [list_id], references: [id], onDelete: Cascade, map: "fk_items_list")
  products_clearance products_clearance @relation(fields: [product_id], references: [product_id], onDelete: Cascade, map: "fk_items_product")

  @@unique([list_id, display_order], map: "uq_items_list_order")
  @@unique([list_id, product_id], map: "uq_items_list_product")
  @@index([list_id], map: "idx_items_list")
  @@index([product_id], map: "idx_items_product")
}

model featured_lists {
  id                  Int                   @id @default(autoincrement()) @db.UnsignedInt
  list_key            String                @unique(map: "uq_featured_lists_key") @db.VarChar(64)
  title               String                @db.VarChar(255)
  subtitle            String?               @db.VarChar(255)
  limit_size          Int?
  created_at          DateTime              @default(now()) @db.Timestamp(0)
  updated_at          DateTime?             @default(now()) @db.Timestamp(0)
  featured_list_items featured_list_items[]
}

model products_clearance {
  product_id          Int                   @id @default(autoincrement()) @db.UnsignedInt
  category_id         Int?                  @default(0) @db.UnsignedInt
  sub_id              Int?                  @default(0) @db.UnsignedInt
  part_id             Int?                  @default(0) @db.UnsignedInt
  product_name        String?               @db.VarChar(255)
  product_brand       String?               @db.VarChar(255)
  product_description String?               @db.Text
  product_picture     String?               @db.Text
  product_sku         String?               @db.VarChar(255)
  product_file        String?               @db.Text
  product_filename    String?               @db.VarChar(255)
  product_price       Decimal?              @db.Decimal(10, 2)
  product_new         Int                   @default(0)
  product_best        Int                   @default(0)
  product_status      Int                   @default(1)
  users_action        Int?
  created_at          DateTime              @default(now()) @db.Timestamp(0)
  updated_at          DateTime?             @default(now()) @db.Timestamp(0)
  product_uom         String?               @db.VarChar(10)
  visible             Boolean               @default(true)
  display_order       Int                   @default(0)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 09 Boolean @default(true) @map("09")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 13 Boolean @default(true) @map("13")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  clearanceSales      Boolean               @default(false)
  clearanceQuantity   Int                   @default(0)
  clearancePrice      Decimal?              @db.Decimal(10, 2)
  expo_status         Int                   @default(0) @db.TinyInt
  expo_price          Decimal               @default(0.00) @db.Decimal(10, 2)
  cat5e               Int                   @default(0) @db.TinyInt
  cat6                Int                   @default(0) @db.TinyInt
  tool_tester         Int                   @default(0) @db.TinyInt
  image_url           String?               @db.VarChar(255)
  discount_label      String?               @db.VarChar(16)
  rating_score        Decimal?              @db.Decimal(3, 2)
  rating_count        Int?                  @default(0) @db.UnsignedInt
  featured_list_items featured_list_items[]

  @@index([product_brand], map: "idx_products_clearance_brand")
  @@index([category_id], map: "idx_products_clearance_category")
  @@index([display_order], map: "idx_products_clearance_display_order")
  @@index([product_sku], map: "idx_products_clearance_sku")
  @@index([product_status], map: "idx_products_clearance_status")
  @@index([visible], map: "idx_products_clearance_visible")
}

model products_meta {
  id         Int      @id @default(1) @db.UnsignedTinyInt
  title      String   @db.VarChar(255)
  subtitle   String   @db.VarChar(255)
  card_parts Json?
  updated_at DateTime @default(now()) @db.Timestamp(0)
}

model ui_categories {
  id                Int                 @id @default(autoincrement()) @db.UnsignedInt
  slug              String              @unique(map: "uq_ui_categories_slug") @db.VarChar(64)
  name              String              @db.VarChar(255)
  image_url         String?             @db.VarChar(255)
  visible           Boolean             @default(true)
  display_order     Int                 @default(0)
  created_at        DateTime            @default(now()) @db.Timestamp(0)
  updated_at        DateTime?           @default(now()) @db.Timestamp(0)
  images_categories images_categories[]

  @@index([display_order], map: "idx_ui_categories_order")
  @@index([visible], map: "idx_ui_categories_visible")
}

model ui_categories_meta {
  id         Int      @id @default(1) @db.UnsignedTinyInt
  title      String   @db.VarChar(255)
  subtitle   String   @db.VarChar(255)
  updated_at DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model images_categories {
  id            Int           @id @default(autoincrement()) @db.UnsignedInt
  category_id   Int           @db.UnsignedInt
  image_name    String        @db.VarChar(255)
  display_order Int           @default(0) @db.UnsignedInt
  visible       Boolean       @default(true)
  ui_categories ui_categories @relation(fields: [category_id], references: [id], onDelete: Cascade, map: "fk_category_id")

  @@index([category_id], map: "fk_category_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model config_setting {
  id            Int      @id @default(autoincrement()) @db.UnsignedInt
  setting_key   String   @unique(map: "uq_setting_key") @db.VarChar(64)
  setting_value String?  @db.VarChar(255)
  module_detail String?  @db.VarChar(64)
  description   String?  @db.VarChar(255)
  updated_at    DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model import_category_batches {
  id              BigInt                         @id @default(autoincrement()) @db.UnsignedBigInt
  category_data   Json
  source_filename String?                        @db.VarChar(255)
  total_records   Int                            @default(0) @db.UnsignedInt
  status          import_category_batches_status @default(PENDING)
  created_at      DateTime                       @default(now()) @db.Timestamp(0)
  processed_at    DateTime?                      @db.Timestamp(0)
  error_details   String?                        @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model images_products {
  id            BigInt  @id @default(autoincrement())
  product_id    BigInt
  image_name    String  @db.VarChar(255)
  display_order Int     @default(0)
  visible       Boolean @default(true)

  @@index([product_id], map: "idx_product_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model import_product_batches {
  id              BigInt                        @id @default(autoincrement())
  product_data    String?                       @db.LongText
  source_filename String                        @db.VarChar(255)
  total_records   Int                           @default(0)
  status          import_product_batches_status @default(PENDING)
  created_at      DateTime                      @default(now()) @db.Timestamp(0)
  processed_at    DateTime?                     @db.Timestamp(0)
  error_details   String?                       @db.Text

  @@index([status], map: "idx_status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model import_batch_logs {
  batch_id            Int                              @id @default(autoincrement()) @db.UnsignedInt
  import_type         String                           @db.VarChar(50)
  filename            String                           @db.VarChar(255)
  uploaded_by_user_id String                           @db.VarChar(50)
  current_step_status String                           @db.VarChar(100)
  overall_status      import_batch_logs_overall_status @default(PENDING)
  start_timestamp     DateTime                         @default(now()) @db.DateTime(0)
  end_timestamp       DateTime?                        @db.DateTime(0)
  last_log_message    String?                          @db.Text

  @@index([import_type], map: "idx_import_type")
  @@index([overall_status], map: "idx_overall_status")
}

model product_conditions {
  id             Int       @id @default(autoincrement())
  pro_id         Int?
  pro_sku        String?   @db.VarChar(50)
  pro_brand      String?   @db.VarChar(50)
  pro_details    String?   @db.Text
  minimum_length String?   @db.VarChar(255)
  sales_type     String?   @db.VarChar(50)
  units_system   String?   @db.VarChar(50)
  num_stock      String?   @db.VarChar(255)
  cut_steps      String?   @db.VarChar(255)
  created_at     DateTime  @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
}

enum discount_rules_frame_mode {
  draw
  image
}

enum discount_rules_frame_object_fit {
  contain
  cover
  stretch
}

enum import_category_batches_status {
  PENDING
  PROCESSED
  ERROR
  FOLDERS_CREATING
  FOLDERS_CREATED
  IMAGES_COPYING
  COMPLETED
}

enum import_product_batches_status {
  PENDING
  PROCESSING
  COMPLETED
  FOLDERS_CREATING
  IMAGES_COPYING
  ERROR
}

enum import_batch_logs_overall_status {
  PENDING
  IN_PROGRESS
  COMPLETE
  FAILED
  ABANDONED
}
